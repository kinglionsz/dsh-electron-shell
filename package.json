{
  "name": "dsh-desktop",
  "version": "0.1.5-rc.2.shell.14-fix3",
  "description": "Unofficial DeepSeek Harness Electron desktop shell: download and use; identical to `dsh web`",
  "author": "dsh-desktop contributors <citrusli@126.com>",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/citrusli2026/dsh-desktop.git"
  },
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "main": "lib/main/index.cjs",
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "bin": {
    "dsh-validate-release": "./bin/dsh-validate-release.mjs"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test \"test/**/*.test.ts\"",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-lines=80 --test-coverage-branches=75 --test-coverage-functions=70 \"test/**/*.test.ts\"",
    "test:e2e": "pnpm run build && playwright test",
    "test:e2e:android": "pnpm run build && playwright test -g @android-simulator",
    "test:e2e:packaged": "node scripts/e2e-packaged.mjs",
    "test:e2e:market:offline": "node scripts/e2e-market.mjs offline",
    "test:e2e:market:real": "node scripts/e2e-market.mjs real",
    "smoke:packaged": "node scripts/smoke-packaged.mjs",
    "smoke:packaged-ui": "DSH_SMOKE_UI=1 node scripts/smoke-packaged.mjs",
    "verify:full": "pnpm run verify && pnpm run test:e2e && pnpm run dist:dir && pnpm run smoke:packaged && pnpm run smoke:packaged-ui && pnpm run test:e2e:market:offline",
    "site:check": "node --check site/assets/app.js && node --check site/assets/star-prompt.js && node scripts/check-site.mjs && node scripts/check-api-downloads.mjs",
    "runtime:check": "node scripts/check-runtime-boundary.mjs",
    "verify": "pnpm run typecheck && pnpm run runtime:check && pnpm run test:coverage && pnpm run site:check && pnpm run build",
    "security:audit": "pnpm --registry=https://registry.npmjs.org audit && pnpm --dir manifest/harness --registry=https://registry.npmjs.org audit --prod",
    "release:check-assets": "node scripts/check-release-assets.mjs",
    "version:check": "node scripts/version.mjs check",
    "version:bump": "node scripts/version.mjs bump",
    "dev": "pnpm run build && electron .",
    "start": "electron .",
    "smoke": "pnpm run build && electron . --smoke-test",
    "gen-icons": "node scripts/gen-icons.mjs",
    "fetch-node": "node scripts/fetch-node.mjs",
    "deploy-harness": "node scripts/deploy-harness.mjs",
    "rebuild-harness-native": "node scripts/rebuild-harness-native.mjs",
    "bootstrap": "pnpm run deploy-harness && pnpm run fetch-node && pnpm run rebuild-harness-native",
    "postinstall": "node scripts/install-electron.mjs",
    "dist": "pnpm run build && electron-builder",
    "dist:dir": "pnpm run build && electron-builder --dir"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^24.0.0",
    "electron": "^44.3.0",
    "electron-builder": "^26.16.1",
    "electron-updater": "^6.8.9",
    "esbuild": "^0.25.0",
    "sharp": "^0.35.4",
    "tar": "^7.0.0",
    "typescript": "^7.0.0"
  }
}
